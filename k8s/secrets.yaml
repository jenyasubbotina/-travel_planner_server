# Replace values before production. Apply after editing: kubectl apply -f k8s/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: travel-planner-secrets
  namespace: travel-planner
type: Opaque
stringData:
  jwt-secret: "replace-with-at-least-32-chars-secret-key!!"
  database-password: "tp_pass"
  postgres-password: "tp_pass"
